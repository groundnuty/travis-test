script:
  - echo "script!!"

deploy:
 provider: pages
 skip_cleanup: true
 github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
 local_dir: site
 on:
   branch: master
    
#deploy:
#  provider: script
#  script: sh aa.sh
